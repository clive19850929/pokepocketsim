diff --git a/README.md b/README.md
new file mode 100644
index 0000000..0cd3364
--- /dev/null
+++ b/README.md
@@ -0,0 +1,38 @@
+# pokepocketsim (Windows / Python 3.10)
+
+## Dependency install (Windows)
+
+```powershell
+python -m venv .venv
+. .venv\Scripts\Activate.ps1
+python -m pip install -U pip
+python -m pip install -r requirements.txt
+```
+
+## Regenerate lock files (pip-tools)
+
+Edit only the `.in` files and regenerate the locks via pip-compile.
+Do **not** hand-edit `requirements.txt`.
+
+```powershell
+. .venv\Scripts\Activate.ps1
+scripts\compile_requirements.ps1
+```
+
+### Torch CPU/GPU policy
+
+* Default is CPU-only torch using `requirements-torch-cpu.in` (uses the PyTorch CPU index).
+* To switch to GPU, create a new torch input file and lock it separately, then recompile the main lock using that constraint.
+
+Example (CUDA 12.1; adjust to your target):
+
+```powershell
+@"
+--extra-index-url https://download.pytorch.org/whl/cu121
+
+torch
+"@ | Set-Content requirements-torch-gpu.in
+
+pip-compile requirements-torch-gpu.in -o requirements-torch-gpu.txt
+pip-compile requirements.in -o requirements.txt -c requirements-torch-gpu.txt
+```
diff --git a/docs/operations.md b/docs/operations.md
index 709ab44..e798eba 100644
--- a/docs/operations.md
+++ b/docs/operations.md
@@ -6,25 +6,28 @@
    ```powershell
    python -m pip install -U pip pip-tools
    ```
-2. Update pinned requirements:
+2. Update pinned requirements (CPU torch by default):
    ```powershell
-   pip-compile requirements.in -o requirements.txt
+   pip-compile requirements-torch-cpu.in -o requirements-torch-cpu.txt
+   pip-compile requirements.in -o requirements.txt -c requirements-torch-cpu.txt
    ```
    Or run the helper script:
    ```powershell
-   scripts\\compile_requirements.ps1
+   scripts\compile_requirements.ps1
    ```
 3. Install from pinned requirements:
    ```powershell
    python -m pip install -r requirements.txt
    ```
 
+Note: `requirements.txt` is generated by pip-compile. Edit `.in` files only.
+
 ## Diff + log collection (Cursor)
 
 After changes, save the diff so it can be attached with Cursor logs/debug dumps:
 
 ```powershell
-git diff > fixes\\issue_YYYYMMDD_short_desc.diff
+git diff > fixes\issue_YYYYMMDD_short_desc.diff
 ```
 
-Attach `fixes\\*.diff` together with `run_*.log` and any `debug_dump_*.json` files when reporting issues.
+Attach `fixes\*.diff` together with `run_*.log` and any `debug_dump_*.json` files when reporting issues.
diff --git a/requirements-torch-cpu.in b/requirements-torch-cpu.in
new file mode 100644
index 0000000..c75649d
--- /dev/null
+++ b/requirements-torch-cpu.in
@@ -0,0 +1,3 @@
+--extra-index-url https://download.pytorch.org/whl/cpu
+
+torch
diff --git a/requirements.in b/requirements.in
index b46ed35..3d2f3da 100644
--- a/requirements.in
+++ b/requirements.in
@@ -1,5 +1,4 @@
 numpy
-torch
 d3rlpy
 tqdm
 pygame
diff --git a/scripts/compile_requirements.ps1 b/scripts/compile_requirements.ps1
index 508381a..c8197a7 100644
--- a/scripts/compile_requirements.ps1
+++ b/scripts/compile_requirements.ps1
@@ -1,4 +1,21 @@
 $ErrorActionPreference = "Stop"
 
+$repoRoot = Resolve-Path "$PSScriptRoot\.."
+$venvActivate = Join-Path $repoRoot ".venv\Scripts\Activate.ps1"
+
+if (Test-Path $venvActivate) {
+    Write-Host "[venv] Activating $venvActivate"
+    . $venvActivate
+} else {
+    Write-Host "[venv] .venv not found. Create one per README.md before running this script."
+}
+
+$pyInfo = python -c "import sys; print(f'{sys.executable} (Python {sys.version.split()[0]})')"
+Write-Host "[python] $pyInfo"
+
 python -m pip install -U pip pip-tools
-pip-compile requirements.in -o requirements.txt
+
+Push-Location $repoRoot
+pip-compile requirements-torch-cpu.in -o requirements-torch-cpu.txt
+pip-compile requirements.in -o requirements.txt -c requirements-torch-cpu.txt
+Pop-Location
